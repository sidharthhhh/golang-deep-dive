# NOTE: In production, use proper secret management (e.g., Sealed Secrets, Vault).
# This file should NOT be committed to git. See secrets.yaml.example instead.

apiVersion: v1
kind: Secret
metadata:
  name: auth-secrets
type: Opaque
stringData:
  mysql-user: "root"
  mysql-password: "CHANGE_THIS_PASSWORD"
  jwt-secret: "CHANGE_THIS_TO_A_SECURE_32_CHAR_SECRET"
  super-admin-code: "CHANGE_THIS_SUPER_ADMIN_CODE"
