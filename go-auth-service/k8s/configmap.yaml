apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-config
data:
  mysql-host: "mysql-service"
  mysql-port: "3306"
  mysql-database: "auth_service"
  cors-allowed-origins: "https://app.yourdomain.com,https://admin.yourdomain.com"
